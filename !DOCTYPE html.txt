<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>MISALES | Smart Shopping For Modern Living</title>
    <meta name="description" content="Discover Electronics, Home Appliances & Interior Decor Designed For Your Lifestyle. Shop premium products at MISALES today.">
    <meta name="theme-color" content="#7B2FF7">
    
    <!-- SEO Schema Markup -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "MISALES",
      "url": "https://www.misales.com",
      "potentialAction": {
        "@type": "SearchAction",
        "target": "https://www.misales.com/search?q={search_term_string}",
        "query-input": "required name=search_term_string"
      }
    }
    </script>

    <!-- Google Fonts & FontAwesome -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* CSS Variables */
        :root {
            --primary: #7B2FF7;
            --primary-hover: #6320d4;
            --white: #FFFFFF;
            --black: #000000;
            --bg-light: #F5F5F7;
            --text-gray: #6E6E73;
            --border: #E5E5EA;
            --shadow-sm: 0 2px 8px rgba(0,0,0,0.04);
            --shadow-md: 0 8px 24px rgba(0,0,0,0.08);
            --shadow-glow: 0 8px 24px rgba(123, 47, 247, 0.25);
            --radius-md: 12px;
            --radius-lg: 20px;
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        }

        /* Reset & Base Setup */
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Inter', sans-serif; -webkit-tap-highlight-color: transparent; }
        body { background-color: var(--bg-light); color: var(--black); line-height: 1.5; overflow-x: hidden; }
        a { text-decoration: none; color: inherit; }
        ul { list-style: none; }
        img { max-width: 100%; height: auto; border-radius: inherit; }

        /* Typography */
        h1, h2, h3 { letter-spacing: -0.02em; }
        .section-title { font-size: 1.5rem; font-weight: 700; margin-bottom: 1.5rem; padding: 0 1rem; }

        /* Header & Navigation */
        .header { position: sticky; top: 0; background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(10px); z-index: 1000; box-shadow: var(--shadow-sm); padding: 0.75rem 1rem; }
        .nav-top { display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.75rem; }
        .logo { font-size: 1.5rem; font-weight: 800; color: var(--primary); letter-spacing: -1px; }
        .nav-icons { display: flex; gap: 1rem; font-size: 1.25rem; color: var(--black); }
        .nav-icons i { cursor: pointer; transition: var(--transition); }
        .nav-icons i:hover { color: var(--primary); }
        .search-bar { display: flex; background: var(--bg-light); border-radius: var(--radius-md); padding: 0.5rem 1rem; align-items: center; }
        .search-bar input { border: none; background: transparent; width: 100%; outline: none; margin-left: 0.5rem; font-size: 0.95rem; }

        /* Hero Section */
        .hero { position: relative; padding: 4rem 1rem; text-align: center; color: var(--white); background: linear-gradient(135deg, #000000, #3a0ca3, #7B2FF7); background-size: 200% 200%; animation: gradientBG 10s ease infinite; border-radius: 0 0 24px 24px; margin-bottom: 2rem; overflow: hidden; }
        @keyframes gradientBG { 0% {background-position: 0% 50%;} 50% {background-position: 100% 50%;} 100% {background-position: 0% 50%;} }
        .hero h1 { font-size: 2.5rem; font-weight: 800; margin-bottom: 1rem; line-height: 1.1; }
        .hero p { font-size: 1.1rem; opacity: 0.9; margin-bottom: 2rem; max-width: 600px; margin-inline: auto; }
        .hero-btns { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
        .btn { padding: 0.8rem 1.5rem; border-radius: 30px; font-weight: 600; cursor: pointer; border: none; transition: var(--transition); font-size: 0.95rem; }
        .btn-primary { background: var(--primary); color: var(--white); box-shadow: var(--shadow-glow); }
        .btn-primary:hover { background: var(--primary-hover); transform: translateY(-2px); }
        .btn-white { background: var(--white); color: var(--black); }
        .btn-white:hover { background: #f0f0f0; transform: translateY(-2px); }

        /* Categories Grid */
        .categories { padding: 1rem; display: flex; gap: 1rem; overflow-x: auto; scroll-snap-type: x mandatory; margin-bottom: 2rem; -ms-overflow-style: none; scrollbar-width: none; }
        .categories::-webkit-scrollbar { display: none; }
        .category-card { min-width: 100px; background: var(--white); padding: 1rem; border-radius: var(--radius-md); text-align: center; box-shadow: var(--shadow-sm); scroll-snap-align: start; cursor: pointer; transition: var(--transition); border: 1px solid transparent; }
        .category-card:hover { border-color: var(--primary); box-shadow: var(--shadow-glow); transform: translateY(-3px); }
        .category-card span { font-size: 1.5rem; display: block; margin-bottom: 0.5rem; }
        .category-card p { font-size: 0.8rem; font-weight: 500; }

        /* Vertical Shop List (Mobile Optimized) */
        .product-list { padding: 0 1rem; display: grid; gap: 1rem; margin-bottom: 3rem; }
        .product-card { background: var(--white); border-radius: var(--radius-lg); padding: 1rem; display: flex; gap: 1rem; box-shadow: var(--shadow-sm); transition: var(--transition); position: relative; }
        .product-card:hover { box-shadow: var(--shadow-md); }
        .product-img { width: 100px; height: 100px; background: var(--bg-light); border-radius: var(--radius-md); flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-size: 2rem; color: var(--text-gray); }
        .product-info { flex-grow: 1; display: flex; flex-direction: column; justify-content: space-between; }
        .badge { position: absolute; top: -5px; left: -5px; background: var(--black); color: var(--white); font-size: 0.7rem; font-weight: 700; padding: 0.2rem 0.6rem; border-radius: 10px; z-index: 2; }
        .badge-sale { background: #FF3B30; }
        .product-title { font-weight: 600; font-size: 0.95rem; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; margin-bottom: 0.25rem; }
        .product-rating { color: #FF9500; font-size: 0.75rem; margin-bottom: 0.25rem; }
        .product-price { font-weight: 700; font-size: 1.1rem; color: var(--primary); }
        .stock-indicator { font-size: 0.7rem; color: #34C759; margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.25rem; }
        .product-actions { display: flex; gap: 0.5rem; margin-top: 0.5rem; }
        .btn-sm { flex: 1; padding: 0.5rem; font-size: 0.8rem; border-radius: var(--radius-md); text-align: center; }

        /* Horizontal Swipe Carousel */
        .carousel-container { padding: 0 1rem; margin-bottom: 3rem; }
        .carousel { display: flex; gap: 1rem; overflow-x: auto; scroll-snap-type: x mandatory; padding-bottom: 1rem; -ms-overflow-style: none; scrollbar-width: none; }
        .carousel::-webkit-scrollbar { display: none; }
        .carousel-card { min-width: 220px; background: var(--white); border-radius: var(--radius-lg); padding: 1rem; box-shadow: var(--shadow-sm); scroll-snap-align: start; position: relative; }
        .carousel-card .product-img { width: 100%; height: 150px; margin-bottom: 1rem; }
        
        /* Premium Features (Icons) */
        .features { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; padding: 0 1rem; margin-bottom: 3rem; }
        .feature-card { background: var(--white); padding: 1.25rem; border-radius: var(--radius-md); text-align: center; box-shadow: var(--shadow-sm); }
        .feature-card i { font-size: 1.5rem; color: var(--primary); margin-bottom: 0.5rem; }
        .feature-card h4 { font-size: 0.9rem; font-weight: 600; }

        /* Promo Banner */
        .promo-banner { margin: 0 1rem 3rem; background: linear-gradient(45deg, var(--primary), #b5179e); color: var(--white); border-radius: var(--radius-lg); padding: 2rem 1.5rem; display: flex; justify-content: space-between; align-items: center; position: relative; overflow: hidden; box-shadow: var(--shadow-glow); }
        .promo-content h3 { font-size: 1.5rem; margin-bottom: 0.5rem; }
        .promo-content p { font-size: 0.9rem; margin-bottom: 1rem; opacity: 0.9; }

        /* Testimonials */
        .testimonials { padding: 0 1rem; margin-bottom: 3rem; display: flex; gap: 1rem; overflow-x: auto; scroll-snap-type: x mandatory; }
        .testimonials::-webkit-scrollbar { display: none; }
        .review-card { min-width: 280px; background: var(--white); padding: 1.5rem; border-radius: var(--radius-lg); box-shadow: var(--shadow-sm); scroll-snap-align: start; }
        .reviewer { display: flex; align-items: center; gap: 1rem; margin-bottom: 1rem; }
        .avatar { width: 40px; height: 40px; background: var(--primary); color: var(--white); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: 600; }
        .review-text { font-size: 0.9rem; color: var(--text-gray); font-style: italic; }

        /* Newsletter */
        .newsletter { background: var(--white); margin: 0 1rem 3rem; padding: 2rem 1.5rem; border-radius: var(--radius-lg); text-align: center; box-shadow: var(--shadow-sm); }
        .newsletter h3 { margin-bottom: 0.5rem; }
        .newsletter p { font-size: 0.9rem; color: var(--text-gray); margin-bottom: 1.5rem; }
        .input-group { display: flex; gap: 0.5rem; }
        .input-group input { flex: 1; padding: 0.8rem 1rem; border: 1px solid var(--border); border-radius: var(--radius-md); outline: none; }
        .input-group input:focus { border-color: var(--primary); }

        /* Footer */
        .footer { background: var(--white); padding: 3rem 1.5rem 6rem; border-top: 1px solid var(--border); }
        .footer-grid { display: grid; grid-template-columns: 1fr; gap: 2rem; margin-bottom: 2rem; }
        .footer-col h4 { font-size: 1.1rem; margin-bottom: 1rem; color: var(--black); }
        .footer-col ul li { margin-bottom: 0.5rem; color: var(--text-gray); font-size: 0.9rem; transition: var(--transition); cursor: pointer; }
        .footer-col ul li:hover { color: var(--primary); }
        .social-links { display: flex; gap: 1rem; margin-top: 1rem; }
        .social-links i { font-size: 1.25rem; color: var(--text-gray); transition: var(--transition); cursor: pointer; }
        .social-links i:hover { color: var(--primary); }
        .footer-bottom { text-align: center; padding-top: 2rem; border-top: 1px solid var(--border); color: var(--text-gray); font-size: 0.8rem; }

        /* Mobile Sticky Bottom Nav */
        .bottom-nav { position: fixed; bottom: 0; left: 0; width: 100%; background: rgba(255, 255, 255, 0.98); backdrop-filter: blur(10px); border-top: 1px solid var(--border); display: flex; justify-content: space-around; padding: 0.75rem 0; z-index: 1000; padding-bottom: calc(0.75rem + env(safe-area-inset-bottom)); }
        .nav-item { display: flex; flex-direction: column; align-items: center; gap: 0.25rem; color: var(--text-gray); font-size: 0.7rem; cursor: pointer; transition: var(--transition); }
        .nav-item.active, .nav-item:hover { color: var(--primary); }
        .nav-item i { font-size: 1.25rem; }
        
        /* Desktop/Tablet Media Queries */
        @media (min-width: 768px) {
            .product-list { grid-template-columns: repeat(2, 1fr); }
            .features { grid-template-columns: repeat(4, 1fr); }
            .footer-grid { grid-template-columns: repeat(4, 1fr); }
            .bottom-nav { display: none; }
            .footer { padding-bottom: 3rem; }
            .header { padding: 1rem 5%; }
            .hero, .product-list, .carousel-container, .features, .promo-banner, .testimonials, .newsletter, .categories { margin-left: 5%; margin-right: 5%; }
        }
        @media (min-width: 1024px) {
            .product-list { grid-template-columns: repeat(3, 1fr); }
        }

        /* Animations */
        .fade-in { opacity: 0; transform: translateY(20px); transition: opacity 0.6s ease-out, transform 0.6s ease-out; }
        .fade-in.visible { opacity: 1; transform: translateY(0); }
    </style>
</head>
<body>

    <!-- Top Sticky Header -->
    <header class="header">
        <div class="nav-top">
            <div class="logo">MISALES.</div>
            <div class="nav-icons">
                <i class="fa-solid fa-heart"></i>
                <i class="fa-solid fa-user"></i>
                <i class="fa-solid fa-cart-shopping"></i>
            </div>
        </div>
        <div class="search-bar">
            <i class="fa-solid fa-magnifying-glass" style="color: var(--text-gray)"></i>
            <input type="text" placeholder="Search electronics, home decor...">
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero fade-in">
        <h1>Smart Shopping<br>For Modern Living</h1>
        <p>Discover Electronics, Home Appliances & Interior Decor Designed For Your Lifestyle.</p>
        <div class="hero-btns">
            <button class="btn btn-white">Shop Now</button>
            <button class="btn btn-primary">Explore Categories</button>
        </div>
    </section>

    <!-- Categories -->
    <div class="categories fade-in">
        <div class="category-card">
            <span>📱</span>
            <p>Electronics</p>
        </div>
        <div class="category-card">
            <span>🍳</span>
            <p>Home & Kitchen</p>
        </div>
        <div class="category-card">
            <span>🏠</span>
            <p>Interior Decor</p>
        </div>
        <div class="category-card">
            <span>💡</span>
            <p>Smart Home</p>
        </div>
        <div class="category-card" style="border-color: var(--primary);">
            <span>🎁</span>
            <p>Offers</p>
        </div>
    </div>

    <!-- Vertical Product List (Thumb-friendly) -->
    <h2 class="section-title fade-in">Trending Now</h2>
    <div class="product-list fade-in">
        <!-- Product 1 -->
        <div class="product-card">
            <span class="badge">Bestseller</span>
            <div class="product-img"><i class="fa-solid fa-headphones"></i></div>
            <div class="product-info">
                <div>
                    <h3 class="product-title">Aura Pro Noise-Cancelling Headphones</h3>
                    <div class="product-rating">
                        <i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star-half-stroke"></i>
                        <span style="color: var(--text-gray); font-size: 0.7rem;">(124)</span>
                    </div>
                    <p class="product-price">$249.99</p>
                </div>
                <div>
                    <div class="stock-indicator"><i class="fa-solid fa-circle-check"></i> In Stock</div>
                    <div class="product-actions">
                        <button class="btn btn-white btn-sm" style="border: 1px solid var(--primary); color: var(--primary);">Add to Cart</button>
                        <button class="btn btn-primary btn-sm">Buy Now</button>
                    </div>
                </div>
            </div>
        </div>
        <!-- Product 2 -->
        <div class="product-card">
            <span class="badge badge-sale">-20% Sale</span>
            <div class="product-img"><i class="fa-solid fa-blender"></i></div>
            <div class="product-info">
                <div>
                    <h3 class="product-title">SmartBlend Pro 800W Kitchen System</h3>
                    <div class="product-rating">
                        <i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-regular fa-star"></i>
                        <span style="color: var(--text-gray); font-size: 0.7rem;">(89)</span>
                    </div>
                    <p class="product-price">$129.50 <span style="text-decoration: line-through; color: var(--text-gray); font-size: 0.8rem; font-weight: 400;">$160.00</span></p>
                </div>
                <div>
                    <div class="stock-indicator"><i class="fa-solid fa-circle-check"></i> In Stock</div>
                    <div class="product-actions">
                        <button class="btn btn-white btn-sm" style="border: 1px solid var(--primary); color: var(--primary);">Add to Cart</button>
                        <button class="btn btn-primary btn-sm">Buy Now</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Horizontal Swipe Carousel -->
    <div class="carousel-container fade-in">
        <h2 class="section-title" style="padding: 0; margin-bottom: 1rem;">New Arrivals</h2>
        <div class="carousel">
            <div class="carousel-card">
                <span class="badge" style="background: var(--primary);">New</span>
                <div class="product-img"><i class="fa-solid fa-lightbulb" style="font-size: 3rem; color: var(--text-gray);"></i></div>
                <h3 class="product-title">Lumiere Smart Bulb 4-Pack</h3>
                <p class="product-price">$45.00</p>
            </div>
            <div class="carousel-card">
                <div class="product-img"><i class="fa-solid fa-couch" style="font-size: 3rem; color: var(--text-gray);"></i></div>
                <h3 class="product-title">Modern Minimalist Accent Chair</h3>
                <p class="product-price">$315.00</p>
            </div>
            <div class="carousel-card">
                <div class="product-img"><i class="fa-solid fa-tv" style="font-size: 3rem; color: var(--text-gray);"></i></div>
                <h3 class="product-title">Ultra HD 4K Smart Monitor</h3>
                <p class="product-price">$499.00</p>
            </div>
        </div>
    </div>

    <!-- Promo Banner -->
    <div class="promo-banner fade-in">
        <div class="promo-content">
            <h3>Flash Sale Weekend</h3>
            <p>Up to 50% off select Smart Home devices.</p>
            <button class="btn btn-white" style="padding: 0.5rem 1rem;">Claim Offer</button>
        </div>
        <i class="fa-solid fa-bolt" style="font-size: 4rem; opacity: 0.2; position: absolute; right: -10px; bottom: -10px;"></i>
    </div>

    <!-- Trust Features -->
    <div class="features fade-in">
        <div class="feature-card">
            <i class="fa-solid fa-truck-fast"></i>
            <h4>Fast Delivery</h4>
        </div>
        <div class="feature-card">
            <i class="fa-solid fa-shield-halved"></i>
            <h4>Secure Pay</h4>
        </div>
        <div class="feature-card">
            <i class="fa-solid fa-gem"></i>
            <h4>Premium</h4>
        </div>
        <div class="feature-card">
            <i class="fa-solid fa-headset"></i>
            <h4>24/7 Support</h4>
        </div>
    </div>

    <!-- Testimonials -->
    <h2 class="section-title fade-in">Loved By Customers</h2>
    <div class="testimonials fade-in">
        <div class="review-card">
            <div class="reviewer">
                <div class="avatar">SJ</div>
                <div>
                    <h4 style="font-size: 0.9rem;">Sarah Jenkins</h4>
                    <div class="product-rating"><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i></div>
                </div>
            </div>
            <p class="review-text">"The interface is incredibly smooth, and my smart home accessories arrived in just 2 days. Highly recommended platform!"</p>
        </div>
        <div class="review-card">
            <div class="reviewer">
                <div class="avatar" style="background: var(--black);">MT</div>
                <div>
                    <h4 style="font-size: 0.9rem;">Marcus Torres</h4>
                    <div class="product-rating"><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i></div>
                </div>
            </div>
            <p class="review-text">"Buying interior decor online is usually a hassle, but MISALES made it so easy. Premium quality straight out of the box."</p>
        </div>
    </div>

    <!-- Newsletter -->
    <div class="newsletter fade-in">
        <h3>Get Exclusive Deals First</h3>
        <p>Subscribe to our newsletter and get 10% off your first purchase.</p>
        <div class="input-group">
            <input type="email" placeholder="Enter your email address">
            <button class="btn btn-primary">Subscribe</button>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer fade-in">
        <div class="footer-grid">
            <div class="footer-col">
                <div class="logo" style="margin-bottom: 1rem;">MISALES.</div>
                <p style="color: var(--text-gray); font-size: 0.9rem;">Smart shopping for modern living. Designed for your lifestyle.</p>
                <div class="social-links">
                    <i class="fa-brands fa-instagram"></i>
                    <i class="fa-brands fa-twitter"></i>
                    <i class="fa-brands fa-facebook-f"></i>
                </div>
            </div>
            <div class="footer-col">
                <h4>Categories</h4>
                <ul>
                    <li>Electronics</li>
                    <li>Home & Kitchen</li>
                    <li>Interior Decor</li>
                    <li>Smart Home</li>
                </ul>
            </div>
            <div class="footer-col">
                <h4>Support</h4>
                <ul>
                    <li>Contact Us</li>
                    <li>Order Tracking</li>
                    <li>Returns Policy</li>
                    <li>Customer Service</li>
                </ul>
            </div>
            <div class="footer-col">
                <h4>Legal</h4>
                <ul>
                    <li>Terms & Conditions</li>
                    <li>Privacy Policy</li>
                    <li>Cookie Policy</li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            &copy; 2026 MISALES. All rights reserved. Premium E-Commerce Platform.
        </div>
    </footer>

    <!-- Sticky Mobile Bottom Navigation -->
    <nav class="bottom-nav">
        <div class="nav-item active">
            <i class="fa-solid fa-house"></i>
            <span>Home</span>
        </div>
        <div class="nav-item">
            <i class="fa-solid fa-border-all"></i>
            <span>Categories</span>
        </div>
        <div class="nav-item">
            <i class="fa-solid fa-cart-shopping"></i>
            <span>Cart</span>
        </div>
        <div class="nav-item">
            <i class="fa-solid fa-user"></i>
            <span>Profile</span>
        </div>
    </nav>

    <!-- Interactions & Animations Script -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Intersection Observer for smooth fade-in animations on scroll
            const observerOptions = {
                root: null,
                rootMargin: '0px',
                threshold: 0.15
            };

            const observer = new IntersectionObserver((entries, observer) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        observer.unobserve(entry.target);
                    }
                });
            }, observerOptions);

            document.querySelectorAll('.fade-in').forEach((el) => {
                observer.observe(el);
            });

            // Bottom Nav Interaction Highlight
            const navItems = document.querySelectorAll('.bottom-nav .nav-item');
            navItems.forEach(item => {
                item.addEventListener('click', function() {
                    navItems.forEach(nav => nav.classList.remove('active'));
                    this.classList.add('active');
                });
            });

            // Optional: Auto-scroll functionality for the horizontal carousel
            // Can be triggered based on user inactivity if desired.
        });
    </script>
</body>
</html>